<!DOCTYPE html>
<html>

    <head>
        <title>Dashboard</title>
    </head>
    <link rel="stylesheet" href="css/login_dashboard.css">

    <body>
        <% if (mtype=="error" ) { %>
            <h3 style="color:green">
                <%= message %>
            </h3>
            <a href="login">Try logging in again</a> <br><br>
            <a href="signup">Sign up if you're new!</a>
            <% } else { %>
                <div class="outer">
                    <div class="inner">
                        <ul>
                            <li style="float:right; border-radius: 50px; background-color: rgb(147, 225, 240); color: black;">
                                <%= person.username %>
                            </li>
                        </ul>
                        <h1>Bienvenue</h1>
                        <div class="listings">
                            <a href="/openings?usrname=<%=person.username%>">Job Openings</a>
                        </div>
                        <br><br>
                        <div class="listings">
                            <a href="/categories?usrname=<%=person.username%>">Job Categories</a>
                        </div>
                        <br>
                        <br>
                        <div class="listings">
                            <a href="login">Logout</a>
                        </div>
                        <% } %>
                    </div>
                </div>

    </body>

</html>